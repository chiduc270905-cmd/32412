export interface Room {
  id: number;
  name: string;
  price: string;
  location: string;
  distance: string;
  rating: number;
  image: string;
  verified: boolean;
  description: string;
  match: number;
}
